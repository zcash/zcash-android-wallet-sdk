[toolchain]
# The single source of truth for the toolchain: CI's setup action reads this
# channel rather than pinning a version of its own.
channel = "1.92.0"
targets = [
    "armv7-linux-androideabi",
    "aarch64-linux-android",
    "i686-linux-android",
    "x86_64-linux-android",
]
